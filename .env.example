# =============================================================
# tcms-ai-platform 环境变量示例
# 复制为 .env 后按需修改。全部可选 —— 默认配置即可离线运行。
# =============================================================

# --- 端口（默认 8000）---
PORT=8000

# --- TCMS 上游目录（可选）---
# 若你希望使用某个 tcms-can-test 的活副本（而非平台内置快照），
# 设为其根目录（含 tcms/ 与 scenarios/）：
# TCMS_UPSTREAM_DIR=D:\path\to\tcms-can-test

# --- LLM API（可选）---
# 当前 Agent 默认用离线 Mock 后端，无需任何 key 即可全功能演示。
# 未来接入真 LLM 时再配置（以下任选其一）：
# DASH_API_KEY=sk-xxx          # 阿里云百炼
# DEEPSEEK_API_KEY=sk-xxx      # DeepSeek
# LLM_API_KEY=sk-xxx

# --- 启动时不自动开浏览器（设为任意值即禁用）---
# DSH_NO_BROWSER=1
